import type { InngestFunction } from "inngest"

/**
 * Register Inngest functions here (booking lifecycle, review reminders, etc.).
 * Foundation milestone: no background jobs yet.
 */
export const functions = [] as InngestFunction.Any[]
